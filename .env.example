# Copy this to .env and fill in your values

# Admin panel password — set this in Netlify env vars too
VITE_ADMIN_PASSWORD=change_me_before_deploy

# Cloudinary — image uploads (safe to expose, these are public)
VITE_CLOUDINARY_CLOUD_NAME=daympzgxr
VITE_CLOUDINARY_UPLOAD_PRESET=portfolio_unsigned
